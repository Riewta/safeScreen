// Hidden: full coupon wallet page — reserved for future use
// Coupon codes are now entered inline in cart / checkout
import { notFound } from "next/navigation";
export default function CouponPage() { return notFound(); }
